apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: team18-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: team18.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: team18-app
                port:
                  number: 80
